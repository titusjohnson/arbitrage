<% if flash.any? %>
  <div class="flash">
    <% flash.each do |type, message| %>
      <div class="flash__message flash__message--<%= type %>">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
