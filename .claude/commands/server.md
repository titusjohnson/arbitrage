---
description: Start the Rails development server
---

Start the Rails development server using `bin/dev` (which runs the server with asset compilation).

The server will be available at http://localhost:3000
